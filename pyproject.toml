[project]
name = "sermetrics"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "jwt",
    "simple-salesforce",
    "pandas",
    "python-dotenv>=1.1.0",
]
